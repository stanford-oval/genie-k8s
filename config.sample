# base image on top of which the actual Kubeflow image is built
# (to speed up rebuilds)
COMMON_IMAGE=

# default version of the Genie components to include in the docker image
genienlp_version=master
genie_version=master
