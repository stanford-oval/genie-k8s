name: Eval
description: |
  Evaluate
inputs:
  - {name: image, description: '', default: ''}
  - {name: project, description: '', default: ''}
  - {name: owner, description: '', default: ''}
  - {name: experiment, description: '', default: ''}
  - {name: canonical, description: '', default: ''}
  - {name: model, description: '', default: ''}
  - {name: model_owner, description: '', default: ''}
  #- {name: eval_set, description: '', default: ''}
  - {name: s3_model_dir, description: '', default: ''}
  - {name: additional_args, description: '', default: ''}
outputs:
  - {name: metrics_output}
  - {name: s3_metrics_output}
  - {name: MLPipeline UI metadata, type: UI metadata}
  - {name: MLPipeline Metrics, type: Metrics}
implementation:
  container:
    image: '{{inputs.parameters.image}}'
    command:
    - /bin/bash
    - -ex
    - -c 
    - |
      . /opt/genie-toolkit/lib.sh
      parse_args "$0" "image project owner experiment canonical model model_owner s3_model_dir s3_metrics_output metrics_output" "$@"
      shift $n
      cd $HOME

      /opt/genie-toolkit/sync-repos.sh 
      MAKEDIR=`get_make_dir ${project}`
      cd workdir/${MAKEDIR}

      cat >> config.mk <<EOF
      developer_key = ${THINGPEDIA_DEVELOPER_KEY}
      EOF

      export TZ=America/Los_Angeles
      export TS_NODE_TRANSPILE_ONLY=true
      npm install
      npm i ts-node
      
      aws s3 sync --no-progress ${s3_model_dir}/${canonical}/eval-synthetic/ ${canonical}/eval-synthetic/
      aws s3 cp --no-progress ${s3_model_dir}/${canonical}/manifest.tt ${canonical}/manifest.tt
      aws s3 sync --no-progress ${s3_model_dir}/models/${model}/ ${canonical}/models/${model}/
      
      make model=${model} eval_set=eval_synthetic experiment=${canonical} evaluate
      
      aws s3 sync --no-progress ${canonical}/eval-synthetic/ ${s3_model_dir}/${canonical}/eval-synthetic/

    args: [
      'cmd',
      --image, {inputValue: image},
      --owner, {inputValue: owner},
      --project, {inputValue: project},
      --experiment, {inputValue: experiment},
      --canonical, {inputValue: canonical},
      --model, {inputValue: model},
      --model_owner, {inputValue: model_owner},
      #--eval_set, {inputValue: eval_set},
      --s3_model_dir, {inputValue: s3_model_dir},
      --s3_metrics_output, {outputPath: s3_metrics_output},
      --metrics_output, {outputPath: metrics_output},
      --, {inputValue: additional_args}
    ]

    fileOutputs:
      MLPipeline UI metadata: /tmp/mlpipeline-ui-metadata.json
      MLPipeline Metrics: /tmp/mlpipeline-metrics.json
