name: Train
description: |
  Training
inputs:
  - {name: image, description: '', default: ''}
  - {name: s3_bucket, description: '', default: ''}
  - {name: owner, description: '', default: ''}
  - {name: dataset_owner, description: '', default: ''}
  - {name: task_name, description: '', default: '' }
  - {name: project, description: '', default: ''}
  - {name: experiment, description: '', default: ''}
  - {name: model, description: '', default: ''}
  - {name: load_from, description: '', default: ''}
  - {name: s3_datadir, description: '', default: ''}
  - {name: train_iterations, description: '', default: ''}
  - {name: skip_tensorboard, description: '', default: 'false'}
  - {name: additional_args, description: '', default: ''}
outputs:
  - {name: s3_model_dir}
implementation:
  container:
    image: '{{inputs.parameters.image}}'
    command:
    - /bin/bash
    - -ex
    - -c
    - |
      . /opt/genie-toolkit/lib.sh

      parse_args "$0" "image s3_bucket owner dataset_owner task_name project experiment model load_from s3_datadir s3_model_dir skip_tensorboard" "$@"
      shift $n 
      cd $HOME
      s3_datadir=`cat $s3_datadir`

      /opt/genie-toolkit/sync-repos.sh 

      modeldir="$HOME/models/$model"
      mkdir -p "$modeldir"

      if ! test  ${load_from} = 'None' ; then
          aws s3 sync ${load_from}/ "$modeldir"/ --exclude "iteration_*.pth" --exclude "*eval/*"  --exclude "*.log"
      fi

      aws s3 sync --exclude "synthetic*.txt" ${s3_datadir} dataset/

      rm -fr "$modeldir/dataset"
      mkdir "$modeldir/dataset"
      rm -fr "$modeldir/cache"
      mkdir -p "$modeldir/cache"
      ln -s "$HOME/dataset" "$modeldir/dataset/almond"
      ln -s $modeldir /home/genie-toolkit/current
      mkdir -p "/shared/tensorboard/${project}/${experiment}/${owner}/${model}"
     
      tensorboard_args=
      if test "$skip_tensorboard" != "true" ; then
        tensorboard_args="--tensorboard_dir /shared/tensorboard/${project}/${experiment}/${owner}/${model}"
      fi
     
      genienlp train \
          --data "$modeldir/dataset" \
          --save "$modeldir" \
          ${tensorboard_args} \
          --cache "$modeldir/cache" \
          --train_tasks ${task_name} \
          --preserve_case \
          --exist_ok \
          --skip_cache \
          $@ 

      rm -fr "$modeldir/cache"
      rm -fr "$modeldir/dataset"
      S3_MODEL_DIR=s3://${s3_bucket}/${owner}/models/${project}/${experiment}/${model}/`date +%s`/
      aws s3 sync ${modeldir}/ ${S3_MODEL_DIR}
            
      mkdir -p `dirname $s3_model_dir`
      echo ${S3_MODEL_DIR} > $s3_model_dir
    
    args: [
      'cmd',
      --image, {inputValue: image},
      --s3_bucket, {inputValue: s3_bucket},
      --owner, {inputValue: owner},
      --dataset_owner, {inputValue: dataset_owner},
      --task_name, {inputValue: task_name},
      --project, {inputValue: project},
      --experiment, {inputValue: experiment},
      --model, {inputValue: model},
      --load_from, {inputValue: load_from},
      --s3_datadir, {inputPath: s3_datadir},
      --s3_model_dir, {outputPath: s3_model_dir},
      --skip_tensorboard, {inputValue: skip_tensorboard},
      --,
      --train_iterations, {inputValue: train_iterations},
      {inputValue: additional_args}, 
    ]
    
